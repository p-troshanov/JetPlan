# Jetplan

## Серверное развёртывание

Серверный контейнерный контур Jetplan управляется через Podman Compose. Во всех командах и runbook следует использовать синтаксис `podman compose` без дефиса, а не отдельную команду `podman-compose`.
